{
  "extends": "../tsconfig.base.node.json",
  "include": ["src"],
  "exclude": ["node_modules"],
  "compilerOptions": {
    "incremental": true,
    "paths": {
      "@/*": ["./src/*"]
    },
    // Must be the same as rollup output dir
    "outDir": "../api",
    "types": ["node", "express"]
  }
}
